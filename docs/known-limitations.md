# דברים שנדחו במכוון

מסמך זה מרכז החלטות תכנון שבהן דחינו משהו בכוונה, כדי שלא ייעלם ונדע למה לחזור.

## אימות במייל (OTP) — פתור
**נפתר ב:** 22 בספטמבר 2026
**מה עשינו:**
1. חיברנו Resend כ-SMTP חיצוני (Supabase Dashboard → Authentication → Emails → SMTP
   Settings): host `smtp.resend.com`, port `465`, username `resend`, password = ה-API key.
2. הוספנו `{{ .Token }}` כטקסט גלוי בגוף ה-HTML של **שתי** תבניות המייל (Authentication →
   Email Templates) — לא רק אחת:
   - **"Confirm sign up"** — זו שנשלחת בפעם הראשונה שמייל מסוים מתחבר דרך המסלול הזה
     (גם אם המייל כבר קיים ב-auth.users דרך Google — זה ספק/flow נפרד).
   - **"Magic link or OTP"** — זו שנשלחת בהתחברות חוזרת עם אותו מייל.
3. אימתנו את הדומיין `apartments770.com` ב-Resend (רשומות DNS דרך Spaceship), ועדכנו
   את כתובת ה-Sender email ב-SMTP Settings ל-`noreply@apartments770.com`. נבדק בפועל:
   OTP מגיע עכשיו לכל כתובת מייל, לא רק לחשבון ה-Resend.

## Apple Sign-In
**נדחה ב:** תחילת שלב ה'
**הבעיה:** דורש הרשמה בתשלום ל-Apple Developer Program (כ-$99/שנה) והגדרה טכנית נוספת.
**מה צריך כדי לחזור לזה:** חשבון Apple Developer, ואז הוספת ספק נוסף ב-Supabase (Google
כבר עובד ולא ייפגע מזה).

## מפה + חישוב מרחק הליכה — מחובר (22 בספטמבר 2026)
**מה עשינו:**
- טופס פרסום דירה: בעל הדירה מסמן מיקום מדויק על מפה (חיפוש כתובת + סיכה נגררת,
  `src/components/LocationPicker.tsx`). אין יותר שדה כתובת חופשי נפרד — הכתובת נקבעת
  אך ורק לפי המיקום על המפה (מתוצאת החיפוש, או geocoding הפוך ממיקום הסיכה אחרי
  גרירה/לחיצה), כדי שלא יהיה פער בין כתובת שהוקלדה לבין המיקום האמיתי.
- עמוד הדירה הציבורי: מפה אינטראקטיבית (Mapbox GL JS, `src/components/Map.tsx`) עם שתי
  סיכות — הדירה + נקודת הציון של האזור — ושורת טקסט עם זמן הליכה בפועל (Mapbox Directions
  API, מסלול רחוב אמיתי, לא קו אווירי).
- פאנל ניהול: שני כפתורי "מילוי קואורדינטות" חד-פעמיים (`/admin/properties`, למעלה) —
  אחד לשני האזורים (שכתובת נקודת הציון שלהם הייתה רק טקסט עד עכשיו), אחד לדירות ישנות
  שפורסמו לפני שהיה picker (geocoding לפי הכתובת הקיימת שלהן — פחות מדויק מה-picker, אבל
  עדיף מכלום).

**פעולה נדרשת ממך:** להוסיף `NEXT_PUBLIC_MAPBOX_TOKEN` (ה-Access Token מחשבון Mapbox שלך)
ב-Vercel Environment Variables (Production + Preview) — בלי זה המפה לא תוצג בכלל (נכשל
בשקט, לא קורס). אחרי ההוספה: Redeploy, ואז ללחוץ על שני כפתורי המילוי החד-פעמי בפאנל
הניהול פעם אחת.

## דירוג וביקורות
**נדחה ב:** מסמך התכנון המקורי (מסומן שלב 2)
**הבעיה:** אין טבלה במסד הנתונים, ואין עדיין הזמנות שהושלמו שאפשר לדרג.
**מה צריך כדי לחזור לזה:** לתכנן ולבנות טבלה + זרימת דירוג, כחלק משלב 2.

## סינון חיפוש לפי תאריכים ספציפיים
**נדחה ב:** שלב ד'
**הבעיה:** אין עדיין טבלת "תאריכים תפוסים/פתוחים" לכל דירה — רק שדה `availability_mode`
כללי על הדירה. החיפוש כרגע מסנן רק לפי אזור ומספר אורחים; התאריכים רק "עוברים הלאה"
להודעת הוואטסאפ.
**מה צריך כדי לחזור לזה:** טבלת זמינות לפי תאריך, וממשק לבעל הדירה לסמן תאריכים תפוסים/פתוחים.

## פאנל ניהול — רק טאב אישור דירות נבנה
**נבנה חלקית ב:** שלב ו' (התחלה)
**מה קיים:** `/admin/login` + `/admin/properties` — טבלת כל הדירות עם סטטוס, טלפון/וואטסאפ
ישיר, ואשר/דחה; עמוד פרטים מלא לכל דירה (`/admin/properties/[id]`) עם כל הנתונים, גלריה,
וגישה ישירה לבעל הדירה. מוגן בסיסמה יחידה (`ADMIN_PASSWORD`, לא קשור ל-Auth הציבורי).

**מודל התשלום (סוכם 22 בספטמבר 2026):** אין סליקה באתר. הבעלים משלם ידנית (העברה/וואטסאפ)
מחוץ למערכת. לחיצת "אשר" על דירה היא גם פעולת אישור התשלום: האדמין בוחר תאריך תוקף חופשי
(ברירת מחדל: שנה מהיום), והמערכת:
1. מעדכנת `owners.subscription_expires_at` (על הבעלים, לא על הדירה הבודדת — תשלום אחד
   מכסה את כל הדירות של אותו בעלים) ומאפסת `is_frozen`.
2. שולחת לבעל הדירה מייל עם PDF מצורף (אישור פרסום + תאריך תוקף), דרך Resend API ישירות
   (`RESEND_API_KEY`) — לא דרך ה-SMTP של Supabase, ששרת רק את מיילי ה-Auth.
3. אם שליחת המייל נכשלת, האישור/התוקף במסד הנתונים **לא** מתבטלים — רק המייל.

**מגבלת דומיין — פתורה (22 בספטמבר 2026):** כמו ב-OTP, גם כאן היה תלוי בדומיין מאומת.
נבדק בפועל אחרי אימות `apartments770.com`: מייל האישור + PDF הגיעו בהצלחה לכתובת שאינה
`ap770official@gmail.com`.

**מגבלה ידועה בייצור ה-PDF:** שם הבעלים ושם האזור נכתבים בעברית באמצעות פונט מוטמע
(Noto Sans Hebrew, subset עברי בלבד ב-`src/lib/fonts/`). אם מישהו יזין שם בעל דירה
באנגלית, התווים הלועזיים לא יוצגו (הפונט לא כולל glyphs לועזיים/ספרות) — מתאים כרגע כי
שמות בקהילה הם כמעט תמיד בעברית, אבל שווה לדעת.

**מה חסר:** 5 הטאבים הנוספים מהמסמך המקורי — תשלומים (כטאב נפרד לצפייה/דוח, בנפרד מפעולת
האישור עצמה), אנליטיקס, תמיכה, בעלי דירות, שוכרים רשומים.

**מוותר במכוון (22 בספטמבר 2026):** אופציה למחוק משתמשים מפאנל הניהול — הוחלט לא לבנות.

## עיצוב חזותי (מותג, לוגו, צבעים)
**נדחה ב:** תחילת הבנייה בפועל (שלב ב')
**ההחלטה:** ממשיכים עם עיצוב Tailwind נקי (שחור/לבן) לאורך כל שלבי הפונקציונליות, וממקדים
ליטוש עיצובי אמיתי בשלב נפרד בסוף (אחרי שלב ז'), כשיהיה למשתמש זמן/לוגו/העדפות צבע.

## שני branches מקבילים
**נרשם ב:** 22 בספטמבר 2026
**המצב:** שני סשנים נפרדים עובדים על שני branches — `claude/sharp-archimedes-dawvfc` וגם
`claude/apartments770-basic-setup-oqzbfi` (מחובר בפועל ל-Vercel). כל קומיט קוד מסונכרן
ידנית (cherry-pick) לשני הצדדים כדי ששניהם יישארו זהים, כולל תיעוד. עדיין לא מוזגו
לbranch אחד קבוע — עד אז, כל סשן שעובד כאן צריך למשוך (`git fetch` + להשוות) לפני שהוא
מתחיל, כדי לא לפספס עבודה שנעשתה במקביל.
